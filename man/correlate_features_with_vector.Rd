% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate_correlation.R
\name{correlate_features_with_vector}
\alias{correlate_features_with_vector}
\title{Correlate each row (feature) of a matrix with a numeric vector.}
\usage{
correlate_features_with_vector(
  mat,
  vec,
  feature_ids = rownames(mat),
  method = c("spearman", "pearson"),
  min_n = 3
)
}
\arguments{
\item{mat}{Numeric matrix (features x samples).}

\item{vec}{Numeric vector (length == ncol(mat)) containing a covariate.}

\item{feature_ids}{Character vector of feature identifiers (length == nrow(mat)).}

\item{method}{Correlation method: "spearman" (default) or "pearson".}

\item{min_n}{Minimum number of non-missing pairs required to compute correlation (default 3).}
}
\value{
A data.frame with columns: feature, cor, pvalue.
}
\description{
Correlate each row (feature) of a matrix with a numeric vector.
}
